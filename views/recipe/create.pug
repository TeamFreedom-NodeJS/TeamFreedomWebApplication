extends ../_layout
//- include ../_mixins/_ingredient

block content
    form(action="/recipes" method="post")
        input.form-control(type="text" name="title" placeholder="Title")
        input.form-control(type="text" name="cookingTimeInMinutes" placeholder="Cooking Time [in Minutes]")
        input.form-control(type="text" name="imageUrls" placeholder="Link To ready Dish Image")
        textarea.form-control(name="preparation" placeholder="Describe Preparation")
        select(name="categories" multiple="multiple")
            each category in categories
                option(value=category.id)
                    = category.name
            
        
        // TO DO: Make ingredients in a cycle with button 'Add INgredient
        //- +ingredient(name="ingredients")
        input.ingredients-names-list(type="text" name="ingredientsNames")
        input.ingredients-quantities-list(type="text" name="ingredientsQuantities")
        input.ingredients-units-list(type="text" name="ingredientsUnits")
        a#btn-add-ingredient-option(href="#")
            | Add ingredient


        //- input(type="text" name="ingredients" placeholder="Име на съставка")
        //- input(type="text" name="ingredients" placeholder="Количество")
        //- select(name="ingredients.units")
        //-     option(value="грам" selected="true")
        //-         | грам
        //-     option(value="мл")
        //-         | мл

        button.btn
            | Хайде